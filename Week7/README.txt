The file dalloc.c shows the dynamic allocation of the array based on user input. It fills the user array with ones.

The file taylor.c prompts the user to enter the order of polynomial of taylors series to use. It will print an output of the value of sum of taylors series for x = 1 (e).

It also prints the difference between the real value of e and the one calculated using taylors series.